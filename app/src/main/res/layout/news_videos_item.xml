<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:padding="8dp">

    <!-- Thumbnail Image -->
    <ImageView
        android:id="@+id/newsVideosItemThumb"
        android:src="@drawable/fragment_header_bg"
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="8dp" />

    <!-- Movie Title -->
    <TextView
        android:id="@+id/newsVideosItemTitle"
        android:textColor="#ff272727"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/newsVideosItemThumb"
        android:layout_toRightOf="@+id/newsVideosItemThumb"
        android:textSize="16sp"
        android:text="Title"
        />

    <!-- Channel -->
    <TextView
        android:id="@+id/newsVideosItemChannel"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:ellipsize="end"
        android:layout_below="@id/newsVideosItemTitle"
        android:layout_marginTop="5dp"
        android:text="Channel"
        android:layout_toRightOf="@+id/newsVideosItemThumb" />

</RelativeLayout>